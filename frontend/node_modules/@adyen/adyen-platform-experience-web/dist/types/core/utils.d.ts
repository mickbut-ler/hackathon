import { DevEnvironment } from './types';
export declare const FALLBACK_ENV = "test";
export declare const FALLBACK_CDN_ENV = "live";
export declare const normalizeLoadingContext: (loadingContext: string) => string;
export declare const normalizeUrl: (url: string) => string;
export declare const resolveEnvironment: (env?: DevEnvironment) => {
    apiUrl: string;
    cdnTranslationsUrl: string;
    cdnAssetsUrl: string;
    cdnConfigUrl: string;
};
export declare const getConfigFromCdn: ({ url }: {
    url: string;
}) => <Fallback>({ name, extension, fallback, subFolder, }: {
    name: string;
    extension?: string;
    subFolder?: string;
    fallback?: Fallback;
}) => Promise<any>;
export declare const getDatasetFromCdn: ({ url }: {
    url: string;
}) => <Fallback>({ name, extension, fallback, subFolder, }: {
    name: string;
    extension?: string;
    subFolder?: string;
    fallback?: Fallback;
}) => Promise<Fallback>;
//# sourceMappingURL=utils.d.ts.map