import { ITransactionCategory } from '../../../../../types';
import { AmountDisplayProps } from '../AmountDisplay/AmountDisplay';
import useTransactionsTotals from '../../hooks/useTransactionsTotals';
import './InsightsTotals.scss';
export interface InsightsTotalsProps {
    currency?: string;
    loadingTotals: boolean;
    totals: ReturnType<typeof useTransactionsTotals>['totalsLookup'];
    error?: Error;
}
interface BreakdownProps extends AmountDisplayProps {
    ariaLabel?: string;
    breakdown: readonly {
        category: ITransactionCategory;
        value: number;
    }[];
}
declare const _default: {
    ({ currency: insightsCurrency, loadingTotals, totals, error }: InsightsTotalsProps): import("preact/compat").JSX.Element;
    Breakdown({ ariaLabel, breakdown, ...amountDisplayProps }: BreakdownProps): import("preact/compat").JSX.Element;
};
export default _default;
//# sourceMappingURL=InsightsTotals.d.ts.map