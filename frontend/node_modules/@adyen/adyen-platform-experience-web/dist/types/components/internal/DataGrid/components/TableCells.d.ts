import { DataGridColumn } from '../types';
import { CustomCell } from '../DataGrid';
import { CustomDataObject } from '../../../types';
import { JSXInternal } from 'preact/src/jsx';
export declare const isCustomDataObject: (item: any) => item is CustomDataObject;
export declare const TableCells: <Items extends Array<any>, Columns extends Array<DataGridColumn<Extract<keyof Items[number], string>>>, CustomCells extends CustomCell<Items, Columns, Columns[number]>>({ columns, customCells, item, rowIndex, }: {
    columns: Columns;
    customCells?: CustomCells;
    item: Items[number];
    rowIndex: number;
}) => JSXInternal.Element;
//# sourceMappingURL=TableCells.d.ts.map