import './SecondaryNav.scss';
import { VNode } from 'preact';
interface SecondaryNavProps<T> {
    className?: string;
    items: T[];
    activeValue: string | null;
    onValueChange: (value: T) => void;
    renderContent: (activeMenu: string) => VNode<any>;
    renderLoadingContent: (activeMenu: string | null) => VNode<any>;
    renderHeader: () => VNode<any>;
    onContentVisibilityChange: (contentVisible: boolean) => void;
    loading?: boolean;
    contentVisible?: boolean;
}
export interface SecondaryNavItem<T extends string = string> {
    value: T;
    label: string;
}
export declare const SecondaryNav: <T extends SecondaryNavItem>({ renderHeader, className, items, activeValue, onValueChange, loading, onContentVisibilityChange, renderContent, contentVisible, renderLoadingContent, }: SecondaryNavProps<T>) => import("preact").JSX.Element;
export {};
//# sourceMappingURL=SecondaryNav.d.ts.map