import { h } from 'preact';
interface CurrencyInputProps {
    amount?: number;
    currency: string;
    currencyItems?: {
        id: string;
        name: string;
    }[];
    hideCurrencySelector?: boolean;
    interactionsDisabled?: boolean;
    isInvalid?: boolean;
    maxValue?: number;
    name?: string;
    onAmountChange: (amount: number) => void;
    onCurrencyChange: (value: string) => void;
    selectedCurrencyCode?: string;
}
export declare const CurrencyInput: ({ amount, currency, currencyItems, hideCurrencySelector, interactionsDisabled, isInvalid, maxValue, name, onAmountChange, onCurrencyChange, selectedCurrencyCode, }: CurrencyInputProps) => h.JSX.Element;
export {};
//# sourceMappingURL=CurrencyInput.d.ts.map