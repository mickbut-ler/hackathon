import { ComponentChildren } from 'preact';
import { TimelineDataList, TimelineStatus, TimelineTagProps, TimelineTimestamp } from '../types';
import './TimelineItem.scss';
export interface TimelineItemProps {
    dataList?: TimelineDataList | null;
    description?: string | null;
    status?: TimelineStatus;
    tag?: TimelineTagProps;
    timestamp: TimelineTimestamp;
    title?: string | null;
    children?: ComponentChildren;
}
export declare function TimelineItem({ dataList, description, status, tag, timestamp, title, children }: TimelineItemProps): import("preact").JSX.Element | null;
//# sourceMappingURL=TimelineItem.d.ts.map