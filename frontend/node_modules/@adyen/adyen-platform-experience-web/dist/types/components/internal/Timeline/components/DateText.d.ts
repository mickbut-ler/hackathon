import { TimelineDateFormat, TimelineDateUnit } from '../types';
interface DateTextProps {
    date: Date;
    format?: TimelineDateFormat;
    timezone?: string;
}
export declare function DateText({ date, format, timezone }: DateTextProps): import("preact").JSX.Element;
export declare function formatDistanceStrict(dateLeft: Date, dateRight: Date, options: {
    unit: TimelineDateUnit;
}): {
    value: number;
    unit: TimelineDateUnit;
};
export {};
//# sourceMappingURL=DateText.d.ts.map