import { parseDate as s } from "../../../utils/datetime/main.js";
const d = 10, r = (t) => {
  const n = s(t);
  if (n != null) {
    const o = Date.now(), e = new Date(n);
    if (new Date(e).setHours(e.getHours() - 24) <= o)
      return 7;
    if (new Date(e).setDate(e.getDate() - d) <= o)
      return 3;
  }
  return 1;
}, m = (t) => r(t) > 1;
export {
  d as ACTION_NEEDED_URGENTLY_THRESHOLD_DAYS,
  r as getActionNeededLevel,
  m as isActionNeededUrgently
};
