import { useMemo as u } from "../../../external/preact/hooks/dist/hooks.module.js";
import n from "../../../core/Context/useCoreContext.js";
import { uuid as a } from "../../../utils/random/uuid.js";
const s = (e) => {
  const { i18n: o } = n();
  return u(() => e.map((r) => ({
    key: r.key,
    value: r.value,
    id: r.id || a(),
    label: r.label ?? o.get(r.key),
    type: r.type,
    config: r.config,
    rawValue: r.rawValue,
    render: r.render
  })), [o, e]);
};
export {
  s as useStructuredListItems
};
