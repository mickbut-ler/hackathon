const a = (n) => {
  const t = new FormData();
  for (const [o, e] of n.entries())
    e instanceof File ? t.set(o, e, e.name) : t.set(o, e);
  return t;
}, r = (n) => {
  const t = new FormData();
  for (const [o, e] of Object.entries(n))
    e && t.set(o, e);
  return a(t);
};
export {
  a as cloneFormData,
  r as getThemePayload
};
