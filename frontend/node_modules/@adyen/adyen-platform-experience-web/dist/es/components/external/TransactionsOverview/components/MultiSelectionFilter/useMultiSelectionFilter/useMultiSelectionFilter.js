import { useMemo as k, useCallback as C } from "../../../../../../external/preact/hooks/dist/hooks.module.js";
import { selectionOptionsFor as O } from "./utils.js";
import { listFrom as T } from "../../../../../../utils/collection/main.js";
const w = ({
  filterParam: o,
  filterValues: M,
  filters: n,
  defaultFilters: c,
  updateFilters: m,
  mapFilterOptionName: b
}) => {
  const q = k(() => T((n == null ? void 0 : n[o]) ?? (c == null ? void 0 : c[o]) ?? ""), [c, n, o]), v = k(
    () => M && O(M, b),
    [M, b]
  ), x = C(
    ({ target: S }) => {
      m == null || m({ [o]: (S == null ? void 0 : S.value) || "" });
    },
    [m, o]
  );
  return { selection: q, selectionOptions: v, updateSelection: x };
};
export {
  w as default,
  w as useMultiSelectionFilter
};
