import { FilterParam as E } from "../../../../../types.js";
const S = [E.CURRENCIES, E.CATEGORIES, E.STATUSES], R = Object.freeze(
  Object.fromEntries(S.map((T) => [T, ""]))
);
export {
  R as DEFAULT_TRANSACTIONS_OVERVIEW_MULTI_SELECTION_FILTER_PARAMS,
  S as TRANSACTIONS_OVERVIEW_MULTI_SELECTION_FILTERS
};
