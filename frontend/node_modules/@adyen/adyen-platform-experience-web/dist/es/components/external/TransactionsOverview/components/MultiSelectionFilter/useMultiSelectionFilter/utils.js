import { identity as r } from "../../../../../../utils/common.js";
import { isFunction as i } from "../../../../../../utils/value/is.js";
const p = (e, o) => {
  const n = i(o) ? o : r;
  return Object.freeze(e.map((t) => ({ id: t, name: n(t) ?? t })));
};
export {
  p as selectionOptionsFor
};
