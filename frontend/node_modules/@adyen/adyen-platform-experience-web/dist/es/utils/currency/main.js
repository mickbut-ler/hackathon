import { getDecimalAmount as e, getDivider as m } from "../../core/Localization/amount/amount-util.js";
const r = (t, o) => e(t, o).toFixed(n(o)), n = (t) => Math.log10(m(t));
export {
  r as formatAmount,
  n as getCurrencyExponent
};
