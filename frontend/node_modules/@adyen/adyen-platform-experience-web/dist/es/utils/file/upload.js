const i = (t) => {
  const s = /* @__PURE__ */ new Set();
  if (t != null && t.items)
    for (const n of t.items) {
      if (n.kind !== "file") continue;
      const f = n.getAsFile();
      f && s.add(f);
    }
  return Array.from(
    s.size === 0 ? new Set((t == null ? void 0 : t.files) ?? s) : s
  );
};
export {
  i as getUploadedFilesFromSource
};
