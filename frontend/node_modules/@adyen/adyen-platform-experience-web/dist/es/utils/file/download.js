const d = ({ blob: c, filename: t }) => {
  const e = document.createElement("a"), o = URL.createObjectURL(c), n = () => void setTimeout(() => URL.revokeObjectURL(o), 150);
  e.href = o, e.download = t || "download", e.addEventListener("click", n, { once: !0 }), e.click();
};
export {
  d as downloadBlob
};
