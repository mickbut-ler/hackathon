function o(n) {
  const t = Array.from(n, (e) => String.fromCodePoint(e)).join("");
  return btoa(t);
}
const a = (n) => {
  try {
    const t = JSON.stringify(n), e = o(new TextEncoder().encode(t)), r = new URLSearchParams();
    return r.set("data", e), r;
  } catch {
    return null;
  }
};
export {
  o as bytesToBase64,
  a as encodeAnalyticsEvent
};
